## Task, scope, and boundaries

:codex-annotation{index="1"}

**Selected call.** `$fpf design challenge` was executed against the CAPRMEDIO work-execution design documented in `CA-A-065`, using its completed SoTA harvest and options report. The challenge stress-tests retained options `O1`, `O2`, `O3`, `O5`, and `O6`, conditional `O4`, progressive overlay `A1`, and their proposed common kernel. It does not choose or implement an architecture. **Confidence that this is the requested receiving use: 100%.**

**Proposal.** Route work by `(content role, operation)`: content role determines meaning, authority, and evidence posture, while operation determines context breadth, control, isolation, and replay. Codex carries broad project interpretation, synthesis, reconciliation, planning, and operator-governed authority work. Durable workflow control carries contracted work with explicit state and recovery and may invoke deterministic Tools, bounded agents, isolated evaluators, and an already governed persistence boundary. Task and Result Envelopes, distinct histories, least-privilege grants, provenance, risk-selected independent evaluation, and governed writes form the proposed common kernel.

**Entity of concern and bounded context.** The entity of concern is a future CAPRMEDIO work-execution architecture spanning CAO, RMED, Implementation, Evaluation, and Ops operations. The inspected context is the conceptual control, state, semantic, authority, evidence, recovery, memory, and persistence structure described by the three current carriers. The receiving use is to expose defects and define repair or evidence work before any architecture decision. The decision owner is the operator.

**Current state, exclusions, and stop condition.** `CA-A-065` records an option frontier, not an admitted architecture. No workflow engine, deployment topology, envelope schema, memory policy, evaluator policy, migration route, or result-class write matrix is selected. This review excludes implementation, engine selection, product comparison, new RMED ownership, runtime-performance claims, and project mutation. It stops after returning concern fingerprints, strengths, insufficient-basis statements, and minimal repair or experiment candidates to project authority.

**Evidence frontier.** The inspected project carriers are:

- `.caprmedio/02_analysis/CA-A-065-ANALYSIS_RPRT--compare-caprmedio-work-execution-architectures.md`, SHA-256 `093ce58ea7de2f8f710cee5730a354ba6ea697534c3889d3506bb7e44700efd4`;
- `.caprmedio/fpf-reports/20260826T202459Z-fpf-sota-harvest-broad-context-agent-narrow-workflow.md`, SHA-256 `921bd5cb5cd22f66616e1ec7e5091313920eb8af6f8575784f0cb0cba5cceefe`;
- `fpf-reports/20260826T205431Z-fpf-options-explore-caprmedio-work-execution-architecture.md`, SHA-256 `7c506bf00b5a0579b8a708e8362e7b57c0b101287ca8525ac45ae0cea2e78a49`;
- live requirements `CA-R-1064`, `CA-R-1104`, `CA-R-1107`, `CA-R-1126`, and `CA-R-805` for Tool contracts, selected graph-context routing, MCP invocation contracts, Journal append receipts, and serialized fenced Git mutation.

The repository revision observed for this review is `999b7712362b338a69aeda6536c43938837219ec`. The worktree contains substantial unrelated concurrent changes; this challenge adds only this report. The live FPF toolkit revision is `18d0c5bc8ace1cf5d99e4af2796694dd1de16ece`; all six direct FPF patterns used here are Stable generated carriers from upstream source revision `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, generated 2026-08-26.

**Resolved FPF source and review profile.** The supporting architecture-use entry routes this review to six direct lenses: `A.1.1` for bounded model use and semantic locality; `A.10` for claim-bound evidence and provenance; `A.15` for System, assignment, Method, WorkPlan, Work, and result separation; `A.20` for exact internal-constraint results; `A.21` for gate decisions from complete independent check sets; and `C.32.P2S` for keeping pressure, candidate structure, decision, realization Work, actual structure, evaluation, and feedback distinct. The frozen review profile is `desk-evidence-design-challenge-v1`; it permits architectural inference but no claim of runtime adequacy.

**Campaign handoff.** Campaign `CAPRMEDIO-WEA-2026-08-27` remains at semantic frontier `SF-02: route by (content role, operation)`. The predecessor phase was candidate exploration; the current phase is design challenge. Existing evidence-gap fingerprints `WEA-GAP-01` through `WEA-GAP-08` remain open. This report adds challenge fingerprints `WEA-CH-01` through `WEA-CH-11`. The only allowed next transition is operator disposition of findings, followed by explicitly authorized repair/evidence work or later decision synthesis. No finding is automatically an Atom or implementation commitment.

**Saved report:** `fpf-reports/20260826T212537Z-fpf-design-challenge-caprmedio-work-execution-architecture.md`

## High-confidence results (>=95%)

### Proposal status and decision boundary

The two-axis correction is a valid semantic frontier for the current design work, but it is not by itself an executable routing or authorization rule. The retained option set remains useful for comparison after the challenge; no option is eliminated solely by the findings below. Every retained option needs repairs to the common kernel, and each keeps distinct candidate-specific risks. The project does not yet have sufficient basis to select a steady-state architecture. **State: `concern`. Confidence: 99%.**

### FPF Challenge Findings — ordered by consequence

#### `WEA-CH-01/pair-route-without-admission-contract` — Critical — common kernel

- **State and confidence:** `concern`; 99%.
- **Proposal claim and entity of concern:** `(content role, operation)` is the routing unit for one proposed work occurrence.
- **Context and receiving use:** every Codex, workflow, Tool, bounded-agent, evaluator, or human-gate route that can consume context or cause an effect.
- **Direct FPF basis:** `A.1.1`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, Solution `A.1.1:4.2–4.4`, requires the exact model edition, use locus, applicability, actual use in Work, direct relations, and semantic-locality values rather than a generic context container.
- **Project evidence:** `CA-A-065` makes the pair the classifier, while only option `O3` explicitly adds target frontier, risk, authority, success criteria, and side-effect class. `CA-R-1104` separately requires selected identities, paths, current digests, and a declared selection boundary.
- **Reviewer inference:** the pair is a classification key, not a complete admission contract. The same pair can require different routes when target revision, authority, accepted context, criteria, consequence, or replay obligation changes.
- **Consequence if unresolved:** a route can be internally consistent yet use stale context, receive excess capabilities, run the wrong checks, or acquire a write path that the pair never authorized.
- **Candidate correction or experiment:** introduce a two-step rule: `PairClassification` identifies the semantic family; a versioned `ExecutionAdmissionContract` binds target/frontier, admitted-context manifest, method or Tool edition, actor and assignment, authority/writer class, side-effect class, required checks, gate profile, replay mode, and stop/return conditions. Replay ten representative cells with and without those fields and record route changes.
- **Unchecked dependency and return condition:** the representative routing corpus and result-class authority matrix remain absent. Return to the operator before treating the added fields as canonical RMED.

#### `WEA-CH-02/digest-valid-semantic-invalid-handoff` — Critical — common kernel

- **State and confidence:** `concern`; 99%.
- **Proposal claim and entity of concern:** immutable Task and Result Envelopes safely cross the Codex/workflow/agent/evaluator boundaries.
- **Context and receiving use:** every cross-plane handoff in which a receiving System interprets selected graph content or a produced result.
- **Direct FPF basis:** `A.1.1`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, Solution `A.1.1:4.3–4.4.1`, requires continuity through exact model use and heterogeneous semantic-locality replay; source, target, direction, fit, permitted loss, claim scope, and direct governor must be recoverable.
- **Project evidence:** the common kernel names immutable envelopes and digests; `CA-R-1104` requires canonical identities, paths, current digests, and selection boundary, but the option report does not define a semantic crossing record.
- **Reviewer inference:** a digest proves the bytes being referenced, not that the receiver uses the same meaning, scope, applicability, or allowed loss. A narrow prompt can omit a distinction while remaining digest-consistent.
- **Consequence if unresolved:** bounded execution can pass every mechanical check while implementing or evaluating the wrong project meaning.
- **Candidate correction or experiment:** add a semantic-handoff block that names the relied-on claims, source and target interpretation schemes, intended receiving use, scope/window, preserved invariants, permitted loss, rejected inferences, and return condition. Build counterexamples where two admissible projections share the same source digest but lead to different decisions.
- **Unchecked dependency and return condition:** no exact envelope schema or semantic-validation catalogue exists. Stop at evidence/proposal status until the operator accepts the crossing semantics.

#### `WEA-CH-03/provenance-is-treated-as-admission` — Critical — common kernel, strongest in `O5`

- **State and confidence:** `concern`; 100%.
- **Proposal claim and entity of concern:** causal provenance, event history, Result Envelopes, and receipts preserve project integrity and support governed persistence.
- **Context and receiving use:** any consumer that decides whether a result may be relied on, evaluated, persisted, or promoted into RMED.
- **Direct FPF basis:** `A.10`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, Solution `A.10:4.1–4.6b`, makes the evidence-provenance graph descriptive and claim-bound; authenticity or provenance is not truth, assurance, approval, permission, or authority. It requires bounded reliance and an explicit `RelianceDisposition`.
- **Project evidence:** `O5` makes provenance the primary integration surface; the common kernel binds artifacts and receipts; `CA-R-1126` correctly limits a Journal receipt to the event and carrier mutation, and `CA-R-805` separately governs the Git effect.
- **Reviewer inference:** the existing Tool/Journal design preserves the needed separation, but the proposed envelope/log kernel does not yet make it mandatory. A richly provenanced result can still be false, inapplicable, stale, or unauthorized.
- **Consequence if unresolved:** a log projection or “verified provenance” flag can become a hidden second authority path, especially for RMED admission or replayed writes.
- **Candidate correction or experiment:** require every consequence-bearing result to link separately to the relied-on claim, evidence path, currentness, reliance disposition, exact gate decision, authority relation, and write receipt. Test that deleting any authority or gate reference makes persistence impossible even when provenance is complete.
- **Unchecked dependency and return condition:** applicability to every existing Evaluation, Ops, Plan/work, Tool, and commit carrier has not been scanned. Reuse current owners; create no parallel provenance ontology without that scan.

#### `WEA-CH-04/evaluator-result-collapses-into-gate` — Critical — common kernel, strongest in `O6`

- **State and confidence:** `concern`; 100%.
- **Proposal claim and entity of concern:** independent evaluation selected by risk and authority policy protects high-consequence work before a governed writer acts.
- **Context and receiving use:** deterministic checks, LLM evaluators, human reviews, and any disposition that permits, restricts, or blocks a bounded action.
- **Direct FPF basis:** `A.20` and `A.21`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solutions `A.20:4.1–4.4` and `A.21:4.1–4.6`. `A.20` separates applicability, evaluation state, outcome, and complete required-set summary. `A.21` requires an applicable gate profile, every required check application, explicit mapping, bounded action consequence, and recheck condition; a check result or green display is not permission.
- **Project evidence:** `O6` separates generator, evaluator, gate, and writer, but no exact check-application identity, complete required set, `notRun`/`unknown` behavior, mapping rule, or decision owner is defined. `CA-R-1064` and `CA-R-1107` define structured Tool results, not gate permission.
- **Reviewer inference:** “independent evaluation” can still certify the wrong criterion, share a common-mode model or data source with the generator, omit an unrun check, or be mistaken for write authorization.
- **Consequence if unresolved:** the architecture can produce evaluator collusion, false assurance, order-dependent decisions, or silent pass on missing evidence.
- **Candidate correction or experiment:** define a versioned GateProfile per bounded action, complete required/optional check sets, evaluator-independence dimensions, source outcomes, explicit mappings to `abstain|pass|degrade|block`, action consequences, and expiry. Run seeded criterion omissions and generator/evaluator common-mode failures.
- **Unchecked dependency and return condition:** criteria ownership, accepted evaluator independence, and result-class gate policy remain undeclared. A decision report must not infer them.

#### `WEA-CH-05/replay-resumes-against-wrong-frontier` — Critical — common kernel

- **State and confidence:** `concern`; 99%.
- **Proposal claim and entity of concern:** durable workflow history provides recovery and replay while semantic-session history stays separate.
- **Context and receiving use:** retry, resume, workflow-version migration, provider/model retirement, changed prompts, changed target Atoms, and changed authority policy.
- **Direct FPF basis:** `A.1.1` and `A.21`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solutions `A.1.1:4.3–4.4` and `A.21:4.6`. They require changed model-use structure to be reidentified and a gate result to be recomputed when profile, check identity/result, scope, or window changes.
- **Project evidence:** `CA-R-805` already freezes Initiative, Git base, subject frontier, target digests, lease, and fencing token and requires revalidation and reconciliation before mutation. The proposed common kernel mentions versioning and replay but does not define freeze, refresh, invalidate, or migrate behavior for semantic and authority changes.
- **Reviewer inference:** workflow replay can be operationally deterministic and semantically invalid. Restarting from old event history against current prompts or authority can split control truth; replaying old authority against current targets can split project truth.
- **Consequence if unresolved:** duplicated or unauthorized effects, stale evaluations, irreproducible evidence, or recovery split-brain.
- **Candidate correction or experiment:** adopt the `CA-R-805` precedent for every consequence-bearing resume: bind the sealed semantic and authority frontier, use monotonic fencing, declare `freeze|refresh-and-recheck|invalidate|version-migrate`, and reconcile uncertain effects before retry. Failure-inject at each boundary.
- **Unchecked dependency and return condition:** required replay levels and in-flight compatibility policy remain open under `WEA-GAP-04`. No engine can be selected until this is explicit.

#### `WEA-CH-06/actor-method-work-collapse` — High — common kernel

- **State and confidence:** `concern`; 98%.
- **Proposal claim and entity of concern:** Codex, workflow control, Tools, bounded agents, evaluators, and writers divide work by architecture role.
- **Context and receiving use:** accountability, authority, capability grants, provenance, recovery, and evaluation of one actual work occurrence.
- **Direct FPF basis:** `A.15`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, Solution `A.15:4.1–4.3`, separates local system-role kind, obtaining assignment, holder System, Method, MethodDescription, WorkPlan, Capability, dated Work, record, and result. Only the holder System performs Work; assignment does not imply authority or responsibility.
- **Project evidence:** the options use topology labels such as “workflow owns recovery,” “agent activity,” and “evaluator role,” but do not bind actual holder Systems, assignments, Methods, Work occurrences, and result records. The common envelope does not supply these relations automatically.
- **Reviewer inference:** without these distinctions, a control component can be credited with Work it only scheduled, and a role label can be overread as authority or permission.
- **Consequence if unresolved:** ambiguous accountability, excessive capabilities, broken causal attribution, and policy that cannot distinguish planner, performer, checker, decider, and writer.
- **Candidate correction or experiment:** require an execution-participant record for each route: named System, obtaining assignment if claimed, enacted Method edition, WorkPlan reference, dated Work identity, capabilities, produced result, and separate authority/responsibility refs. Replay a multi-agent failure and verify that attribution survives retries and replacement workers.
- **Unchecked dependency and return condition:** concrete host/worker topology is not selected. Keep this a schema constraint, not an invented runtime ontology.

#### `WEA-CH-07/idempotency-does-not-bound-external-effects` — Critical — common kernel

- **State and confidence:** `concern`; 98%.
- **Proposal claim and entity of concern:** effects are safe when idempotent, previewable, or mediated by a governed writer.
- **Context and receiving use:** nondeterministic agent retries and external or repository mutations across crash boundaries.
- **Direct FPF basis:** `A.15` and `A.10`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solutions `A.15:4.1–4.2` and `A.10:4.3–4.7`. They keep performed Work, result, evidence path, and causal relation separate; a recorded result does not establish that a world-side effect occurred exactly once.
- **Project evidence:** `CA-R-805` already requires a durable outbox, repository-scoped lease, fencing, pre-effect revalidation, and reconciliation of uncertain mutation. `CA-R-1126` binds accepted event identity to exact canonical bytes and returns an existing receipt on safe retry.
- **Reviewer inference:** a generic idempotency key is insufficient for non-transactional effects when the executor crashes after the external effect but before recording its result, or when the same key is reused with changed semantic bytes.
- **Consequence if unresolved:** duplicate commits or external effects, lost effects reported as complete, and nondeterministic retries that violate project integrity.
- **Candidate correction or experiment:** generalize only the proven mechanism shape: stable action identity plus canonical digest, precondition/frontier, fenced effect ownership, external receipt, uncertain-state reconciliation, and declared compensation or operator stop. Run crash injection before effect, after effect, and before receipt recording.
- **Unchecked dependency and return condition:** external systems and compensation policies are not in scope. Any expansion beyond current repository/Journal writers must return for separate authority review.

#### `WEA-CH-08/memory-admission-and-revocation-absent` — High — common kernel, strongest in `O5`

- **State and confidence:** `concern`; 99%.
- **Proposal claim and entity of concern:** authoritative artifacts, broad session history, workflow replay history, derived summaries, and preferences remain distinct stores or namespaces with selective retrieval.
- **Context and receiving use:** context assembly for future Codex sessions and bounded runs.
- **Direct FPF basis:** `A.10` and `A.1.1`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solutions `A.10:4.5–4.6b` and `A.1.1:4.3–4.4`. They require claim-bounded reliance, currentness, contestability, explicit degrade/abstain/reopen dispositions, and continuity through the actual applied model-use structure.
- **Project evidence:** `CA-A-065` rejects ambient cross-run semantic memory and records retention, correction, privacy, and access as an evidence gap. The option report proposes typed stores but no read-admission, correction, revocation, conflict-order, or derived-summary invalidation rule.
- **Reviewer inference:** namespace separation prevents some accidental mixing but not stale-source preference, poisoned summaries, revoked authority, cross-project leakage, or retention of sensitive scratch context.
- **Consequence if unresolved:** memory contamination can silently alter later routing, criteria, and authority interpretation while remaining provenance-rich.
- **Candidate correction or experiment:** define per-store authority, allowed claim uses, source order, freshness, access, retention, correction, revocation, deletion/forgetting, derived-dependency invalidation, and conflict return. Seed a corrected Atom and verify every derived summary and cached context is degraded or reopened.
- **Unchecked dependency and return condition:** the project memory policy is intentionally undecided. No central memory or provenance backbone should be implemented from this review.

#### `WEA-CH-09/progressive-migration-creates-dual-route-authority` — High — `A1`

- **State and confidence:** `concern`; 98%.
- **Proposal claim and entity of concern:** `(content role, operation)` cells can migrate progressively while other cells remain on the current path.
- **Context and receiving use:** activation, rollback, partial deployment, and concurrent old/new executors.
- **Direct FPF basis:** `C.32.P2S` and `A.21`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solution/checks `C.32.P2S:4`, `CC-C32P2S-5–11`, and `A.21:4.3–4.6`. They preserve candidate/selected/expected/actual structures and Work separately, forbid treating selected design as realized structure, and prevent a route or slice label from silently selecting or weakening policy.
- **Project evidence:** `A1` requires proof per cell but does not define one active-route registry, exclusivity, in-flight ownership, rollback frontier, or old-path fencing.
- **Reviewer inference:** a migrated cell can have both direct Codex/Tool execution and a workflow path active, especially during rollback or retries. Both may individually obey local rules while jointly creating two writers or two decision routes.
- **Consequence if unresolved:** duplicate effects, inconsistent evidence, and migration state mistaken for authority.
- **Candidate correction or experiment:** add a cell-route registry with one active route edition, activation owner, cutover frontier, in-flight rule, writer fencing, rollback contract, and proof receipt. Simulate cutover with old and new work concurrently queued.
- **Unchecked dependency and return condition:** the first migration cells and deployment model remain open. Keep `A1` an overlay, not proof that any cell has migrated.

#### `WEA-CH-10/candidate-specific-failure-surfaces` — High — option frontier

- **State and confidence:** `concern`; 97%.
- **Proposal claim and entity of concern:** `O1`–`O6` are eligible mechanism families after satisfying the protected common kernel.
- **Context and receiving use:** retaining structurally distinct candidates without hiding their unique failure modes.
- **Direct FPF basis:** `C.32.P2S`, `A.15`, and `A.21`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solution/checks `C.32.P2S:4`, `CC-C32P2S-4–11`, `A.15:4.1–4.3`, and `A.21:4.1–4.6`. They require candidate plurality, accepted-loss visibility, realization evidence, explicit return/repair, exact performer attribution, and exact gate use rather than a prose winner.
- **Project evidence and reviewer inference:** `O1` can create semantic ping-pong and indefinite escalation because it has no bounded nondeterministic path; `O2` expands capability, credential, variance, and version surfaces per agent activity; `O3` can turn the policy compiler into an unadmitted meta-authority whose conflicts or bugs affect every route; `O4` can fragment authority, credentials, schemas, and recovery across role services; `O5` can conflate semantic history, workflow replay, provenance, and privacy inside one operational backbone; `O6` can suffer generator/evaluator common-mode failure and disproportionate low-risk cost. These are distinct weaknesses, not reasons to merge the candidates.
- **Consequence if unresolved:** the common kernel can make candidates look safer or more equivalent than their actual control and operating structures.
- **Candidate correction or experiment:** keep an option-local hazard register. Test `O1` escalation latency, `O2` capability escape and model-version replay, `O3` policy conflict/currentness, `O4` cross-role transaction and authority drift, `O5` projection/privacy/rebuild, and `O6` criterion omission/common-mode evaluation under the same Task Envelope and authority policy.
- **Unchecked dependency and return condition:** no matched prototype exists. Retain the partial order; do not exclude or select an option from desk inference alone.

#### `WEA-CH-11/observability-cost-and-safe-degradation-unspecified` — High — common kernel

- **State and confidence:** `concern`; 97%.
- **Proposal claim and entity of concern:** complete causal correlation, context manifests, isolated execution, and risk-selected evaluation are shared requirements.
- **Context and receiving use:** production tracing, incident reconstruction, privacy, credential safety, and bounded operation under cost or latency pressure.
- **Direct FPF basis:** `A.10` and `A.21`, source edition `563f4c8e06a319cbd375b66cdbb2df27a5f8b9ef`, inspected Solutions `A.10:4.4–4.6b` and `A.21:4.5`. They require enough recoverable evidence for the bounded claim and use while preserving currentness, privacy, and contestability, and an explicit restricted action for `degrade` rather than an unspecified fallback.
- **Project evidence:** the SoTA and options reports identify cost/latency and memory/privacy gaps, but the common kernel has no minimum-disclosure profile, retention boundary, telemetry budget, timeout consequence, or rule for workflow/evaluator unavailability.
- **Reviewer inference:** pressure to reduce latency can silently bypass isolation or evaluation, while pressure to maximize observability can leak prompts, graph context, credentials, or sensitive artifacts.
- **Consequence if unresolved:** unsafe fallback, cost collapse, hidden loss of assurance, or a provenance system that becomes the largest security surface.
- **Candidate correction or experiment:** define risk-tiered minimum evidence, redaction and access rules, retention, cost/time budgets, and explicit `degrade|block|return-to-Codex|operator-stop` consequences. Benchmark matched routes and inject unavailable evaluator/workflow cases; never treat missing telemetry as pass.
- **Unchecked dependency and return condition:** threat model, data classification, and budget are absent. Return those choices to the operator; do not infer them from an observability goal.

### Strengths within the inspected scope

1. **Two-axis semantic correction — `no concern found within inspected scope`; 99%.** The proposal correctly rejects phase-only and tool-only routing. `A.1.1:4.2–4.4` supports recovering semantic use and locality through exact relations, and `CA-A-065` demonstrates that one Content role can contain both broad and bounded operations. Limit: this supports classification, not complete admission; `WEA-CH-01` remains.
2. **Evidence remains separate from RMED authority — `no concern found within inspected scope`; 100%.** `CA-A-065` explicitly prohibits workflow, agent, evaluator, Implementation, Evaluation, or Ops output from silently becoming RMED. This aligns with `A.10:4.4–4.6b` and `A.21:4.1–4.5`. Limit: exact gate and result-class write matrices are still missing.
3. **Semantic history and replay history remain distinct — `no concern found within inspected scope`; 99%.** The design correctly rejects both one undifferentiated “memory” and the incoherent claim that durable work has no state. `A.1.1` and `A.10` support use-bounded, current, claim-linked context. Limit: store admission and resume currentness need `WEA-CH-05` and `WEA-CH-08` repairs.
4. **Candidate plurality and progressive adoption preserve reversibility — `no concern found within inspected scope`; 98%.** Retaining `O1`, `O2`, `O3`, `O5`, and `O6`, conditional `O4`, and separate overlay `A1` matches `C.32.P2S` plurality and realization boundaries. Limit: `A1` needs exclusive cutover control under `WEA-CH-09`.
5. **Existing Tool, Journal, and commit contracts provide a strong deterministic precedent — `no concern found within inspected scope`; 100%.** `CA-R-1064`, `CA-R-1107`, `CA-R-1126`, and `CA-R-805` already separate machine contracts, results, receipts, dry-run, identity collision, fencing, frontier revalidation, and uncertain-effect reconciliation. These are reusable architectural constraints, not evidence that a general workflow substrate is implemented.

### Unchecked claims and insufficient basis

- **Final architecture selection — `insufficient basis`; 100%.** There are no accepted owner weights, matched prototypes, engine/deployment constraints, or runtime results. Consequence: any winner would be preference disguised as evidence. Return condition: operator accepts decision coordinates and evidence limits after parity experiments.
- **Runtime determinism, recovery, and exactly-once safety — `insufficient basis`; 100%.** The reports are design evidence only. Consequence: conceptual controls may fail under crash, retry, upgrade, or external side effects. Return condition: failure-injection evidence under a frozen contract.
- **Security and credential isolation — `insufficient basis`; 99%.** Least privilege is a protected constraint, but no threat model, data classification, secret topology, or sandbox proof exists. Return condition: bounded security design and adversarial tests.
- **Envelope and provenance compatibility with current carriers — `insufficient basis`; 99%.** The live Tool/Journal/commit carriers provide precedents, not a completed applicability mapping. Return condition: map every proposed field to existing authority or justify one genuinely missing owner.
- **Evaluator independence and criterion completeness — `insufficient basis`; 100%.** Separate prompts or processes do not establish independent evidence. Return condition: declare independence dimensions and run common-mode and criterion-omission tests.
- **Cost/latency and operating viability — `insufficient basis`; 100%.** No tokens, wall time, retries, operator interventions, infrastructure cost, or escaped-defect baseline exists. Return condition: matched telemetry that remains separate from authority decisions.

### Return to project authority

The challenge returns eleven open concern fingerprints and six insufficient-basis statements to the operator. No finding automatically creates an Atom, changes `CA-A-065`, removes an option, selects an engine, or authorizes implementation. The minimal next decision is whether to repair the common kernel on paper first or authorize a matched evidence campaign. A final architecture decision remains out of scope. **Confidence: 100%.**

## Open questions (confidence <95%)

1. **What is the minimum `ExecutionAdmissionContract`? — 92%, probable but unconfirmed.** Best current answer: bind pair classification, target/frontier, admitted claims/context, actor/method, capabilities, side-effect class, checks/gate profile, writer class, replay mode, and stop/return. Missing evidence: representative route corpus and existing-carrier mapping. Consequence: too little permits semantic/authority drift; too much creates unusable overhead. Exact next action: replay ten recent operations and retain only fields that change admission, recovery, or consequence.
2. **How should Task/Result Envelopes map to current CAPRMEDIO carriers? — 89%, uncertain.** Best current answer: treat an envelope as a transport/coordination projection over existing Plan/work, Tool, Evaluation, Ops, Journal, and receipt owners. Missing evidence: field-by-field applicability scan. Consequence: a new envelope ontology could duplicate authority. Exact next action: produce a mapping table with `reuse`, `projection`, `missing`, or `prohibited-overread` disposition for every field.
3. **Which resume policy applies to each route? — 90%, probable but unconfirmed.** Best current answer: deterministic control history may replay, but changed semantic or authority frontiers must freeze, refresh-and-recheck, invalidate, or version-migrate explicitly. Missing evidence: provider/model retirement and in-flight upgrade requirements. Consequence: recovery split-brain. Exact next action: define replay levels and inject changes to target digest, prompt, Tool, criterion, policy, and model during suspended runs.
4. **What belongs in memory, and how is it corrected or revoked? — 87%, uncertain.** Best current answer: keep authoritative artifacts, session events, workflow histories, summaries, and preferences as separately governed sources with claim-bounded retrieval. Missing evidence: retention, privacy, deletion, correction, conflict, and derived-invalidation policy. Consequence: contamination or leakage. Exact next action: write and test one source-admission/revocation matrix before implementing shared memory.
5. **What counts as evaluator independence? — 90%, probable but unconfirmed.** Best current answer: evaluate independence across prompt/context, model/provider, data, tools, credentials, organizational decision owner, and criterion source; select dimensions by consequence. Missing evidence: risk tiers and acceptable common-mode exposure. Consequence: expensive but illusory dual control. Exact next action: seed criterion omissions and shared-model failure modes in a small O6 experiment.
6. **Which cells should migrate first under `A1`? — 92%, probable but unconfirmed.** Best current answer: fixed schema/ID/relation checks, deterministic tests, receipts, and already governed commits, with one exclusive active route. Missing evidence: recent operation inventory and intervention rate. Consequence: an unrepresentative pilot can overstate safety or value. Exact next action: label at least ten recent CAO, RMED, Implementation, Evaluation, and Ops runs by both axes and select low-ambiguity, high-frequency cells.
7. **What is the cost/latency break-even and safe fallback? — 72%, uncertain.** Best current answer: use isolation and dual control in proportion to authority and escaped-defect cost; unavailable assurance should block, degrade explicitly, return to Codex, or stop for the operator. Missing evidence: matched telemetry and defect outcomes. Consequence: overbuilt infrastructure or silent bypass. Exact next action: benchmark broad-only and enveloped routes with injected unavailability under the same tasks and acceptance criteria.
8. **Which retained architecture should CAPRMEDIO choose? — 88%, uncertain.** Best current answer: none can be selected from current evidence; `O1` maximizes clarity, `O2` breadth, `O3` routing flexibility, `O5` reconstruction, and `O6` assurance. Missing evidence: accepted priorities plus matched experiments. Consequence: premature choice can create a meta-system, data-governance burden, or weak assurance. Exact next action: disposition the common-kernel findings, run parity evidence, then separately authorize `$fpf decision synthesize`.

## Skills used

- `$fpf design challenge` — used to freeze the proposal and semantic frontier, inspect it through six direct FPF lenses, preserve stable finding fingerprints and consequence order, distinguish common-kernel from candidate-specific concerns, identify strengths and insufficient basis, and return minimal repairs or experiments without choosing or implementing an architecture.

#### FPF sources consulted (7 read; 7 used)

- `FPF-Knowledge-Graph/00-readme/00_Practical entries.md` — used to confirm the architecture-use entry and route the challenge to the smallest relevant direct-pattern set.
- `FPF-Knowledge-Graph/A_Kernel Architecture Cluster/01_Holon Ontic Foundation (U.Holon and Admitted Holon Kinds)/01_A.01.01 - Bounded Model-Use Structure and DDD Bounded-Context Recovery.md` — used for exact model-use structure, semantic locality, crossing fit/loss, currentness, and context-as-structure rather than container.
- `FPF-Knowledge-Graph/A_Kernel Architecture Cluster/10_Evidence Graph Referring- Claim-Bound Evidence and Provenance Graph/00_A.10 - Evidence Graph Referring- Claim-Bound Evidence and Provenance Graph.md` — used for claim-bound evidence, provenance limits, reliance disposition, currentness, privacy, and authority separation.
- `FPF-Knowledge-Graph/A_Kernel Architecture Cluster/15_System-Role-Method-Work Alignment/00_A.15 - System-Role-Method-Work Alignment.md` — used to separate System, assignment, Method, WorkPlan, dated Work, capability, record, result, authority, and responsibility.
- `FPF-Knowledge-Graph/A_Kernel Architecture Cluster/20_Constraint Validity for Transformation Steps/00_A.20 - Constraint Validity for Transformation Steps.md` — used for exact constraint applicability, evaluated/not-run state, outcomes, complete required-set summaries, and separation from gate consequences.
- `FPF-Knowledge-Graph/A_Kernel Architecture Cluster/21_Gate Decisions from Independent Check Results/00_A.21 - Gate Decisions from Independent Check Results.md` — used for complete independent check sets, profile application, explicit result mappings, bounded action consequences, recheck, and no implicit gate authority.
- `FPF-Knowledge-Graph/C_Kernel Extension Specifications/20_32_Architecture Candidate Synthesis/01_C.32.P2S - Problem-to-Structure Architecturing Unfolding.md` — used for candidate plurality, accepted-loss visibility, decision/description/Work/actual-structure separation, realization evidence, and return or repair boundaries.
