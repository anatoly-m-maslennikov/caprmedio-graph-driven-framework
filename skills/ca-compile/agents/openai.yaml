interface:
  display_name: "CARMADIO Compile"
  short_description: "Compile atomic CARMADIO truth into evergreen specs and plans"
  default_prompt: "Use $ca-compile to reconcile pending atomic artifacts into the affected evergreen specifications and plans."
