# Active PROGRAMMATIC Methods — compact projection

Source: the 23 active Method atoms directly in this PROGRAMMATIC unit's `05_method/`. Child Tool, App, and MCP units, drafts, and archives are excluded. This derived view is not authority; rebuild it when the active source set or content changes.

- **Technology** (`CA-M-110`, `CA-M-221`, `CA-M-280`, `CA-M-281`): Use Python for the admitted Programmatic scope, currently CPython `3.14.*`. Use uv for governed development workflows and one project technical-configuration boundary. Prefer the standard library when comparably clear and reliable; admit mature libraries through bounded Method selections. Use newer Python idioms only for a stated benefit.
- **Design** (`CA-M-157`, `CA-M-158`, `CA-M-159`, `CA-M-160`): Put deterministic decisions in pure functions with explicit inputs. Use objects only for identity across calls or owned state, invariants, resources, lifecycle, or replaceable adapters. Give replaceable boundaries typed contracts. Keep decisions separate from effects; a bounded one-shot effect may be a function.
- **Effects and temporary state** (`CA-M-161`, `CA-M-289`): Validate targets before file or subprocess effects, bound subprocesses, and report partial failure. Route temporary, cache, staging, and build carriers through `.caprmedio_tmp/`; verify same-filesystem atomicity before replacement and do not fall back silently.
- **Source and automation** (`CA-M-162`, `CA-M-164`, `CA-M-282`, `CA-M-283`): Ratchet changed hand-authored Python toward 200 lines per file and 25 logical lines per executable unit; 26–40 needs one coherent job, and larger or over-complex units need bounded exceptions. Externalize large static mappings. Use pinned Ruff (including `C901`) and Mypy profiles; distinguish formatting, lint, typing, and behavior evidence.
- **Settings, diagnosis, and performance** (`CA-M-163`, `CA-M-165`, `CA-M-166`, `CA-M-284`): Read one validated settings snapshot through the shared Reader. Emit structured, sanitized diagnostics under the active logging policy, distinct from the Journal. Measure representative workloads before optimization. Claim compatibility only for declared interfaces and hosts.
- **Evaluation and input validation** (`CA-M-285`, `CA-M-286`): Choose complementary checks by declared failure mode and retain replay context. Validate untrusted structured data once at a bounded Python boundary with Pydantic where its benefit justifies the dependency; do not make it the universal internal model.
- **Operational handoffs** (`CA-M-191`, `CA-M-192`, `CA-M-193`): Bind each mutation to one sealed human-origin Initiative and stable action identity. Reconcile Git and Journal as independent provenance systems before later reliance. Project the complete valid active Tool frontier to MCP without making MCP a second Tool authority.
