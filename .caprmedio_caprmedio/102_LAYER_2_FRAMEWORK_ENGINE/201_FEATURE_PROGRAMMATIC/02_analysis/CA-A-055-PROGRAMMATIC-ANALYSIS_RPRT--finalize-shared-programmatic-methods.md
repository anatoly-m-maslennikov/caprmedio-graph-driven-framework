---
subjects:
  governs:
    occurrent:
      - Shared Programmatic Method Finalization
  depends_on:
    continuant:
      - Programmatic Policy
      - Method Authority
    occurrent:
      - Validation
version: 2
updated_at: 2026-08-30 17:21:33 +0400
relations:
  derived_from:
    - CA-A-052
    - CA-A-053
---
# Finalize shared PROGRAMMATIC Methods

## Frozen Method input

CA-P-070 froze 31 active or draft Methods at 2026-08-23 16:05:00. The set
contained one shared authority, `CA-M-110` version 6 at the Project root; 22
active child-Scope specializations; and eight child-Scope drafts. CA-P-071
accepted the shared policy meanings materialized below and explicitly deferred
platform claims, universal prerequisite admission, numeric performance budgets,
and Journal or Work Journal policy.

`CA-M-110` version 6 is preserved unchanged at
`.caprmedio/05_method/archive/CA-M-110-CORE-IMPL_METHOD--implement-framework-engine-software-in-python@6.md`.
Its version 7 revision has the same Atom ID and serving Requirement, moves to
PROGRAMMATIC, and replaces the stale `SOFTWARE` boundary terminology with the
current PROGRAMMATIC component boundary. The direct descendants `CA-E-250` and
`CA-D-250` continue to point to the unchanged stable Method ID, so no
Evaluation or Delivery carrier changes are required here.

## Final successor-inclusive Method set

The active shared PROGRAMMATIC Methods are:

| Atom ID | Sole governed procedure |
|---|---|
| `CA-M-110` | Select the Python realization and record bounded technical exceptions. |
| `CA-M-157` | Allocate deterministic transformations to functions. |
| `CA-M-158` | Allocate owned state and lifecycle to objects. |
| `CA-M-159` | Define typed contracts at replaceable technical boundaries. |
| `CA-M-160` | Separate deterministic decisions from effects and lifecycle. |
| `CA-M-161` | Bound file and subprocess effects. |
| `CA-M-162` | Ratchet hand-authored Python source boundaries. |
| `CA-M-163` | Emit structured operational diagnostics. |
| `CA-M-164` | Ratchet typing and automation adoption. |
| `CA-M-165` | Measure before optimizing performance. |
| `CA-M-166` | Preserve declared interface compatibility boundaries. |

Each shared Method directly serves current `CA-R-1047` through `method_for`
and directly records `CA-A-053` through `derived_from`. Each applies to Tools,
App backend services, and MCP components at the stated condition; component-
specific procedures remain below PROGRAMMATIC.

The 22 active child-Scope specializations remain current and unmodified:
`CA-M-087`, `CA-M-101` through `CA-M-104`, `CA-M-128`, `CA-M-129`, and
`CA-M-142` through `CA-M-156`. The eight child-Scope drafts remain candidates
and were neither promoted nor replaced. They do not duplicate active shared
authority because their claims remain bounded to their own component or Tool
manager, worker, Hook, or effect surface.

The separate `CA-M--IMPL_METHOD-PROGRAMMATIC--use-uv-as-the-default-python-workflow-frontend.md`
draft was created after the CA-P-070 freeze at 2026-08-23 16:32:00. It remains
an unaccepted candidate: selecting a development-workflow frontend is a
bounded prerequisite and configuration decision, not shared Method authority
established by CA-P-071. This Task neither promotes nor rejects it.

## Validation set and handoff

The final current Method validation set contains 42 carriers: 33 active
Methods—the 11 shared Methods above plus the 22 retained child-Scope
specializations—and nine drafts: the eight retained child-Scope candidates and
the one post-freeze PROGRAMMATIC candidate. No active Method other than the 11
listed above has current Scope PROGRAMMATIC.

Every active shared Method contains an applicability condition, reproducible
procedure, declared outcome, failure or stop condition, and direct typed
lineage to current `CA-R-1047` and accepted-decision Analysis `CA-A-053`.
No new Evaluation, Delivery, Implementation, configuration selection, platform
claim, numeric performance budget, Journal policy, or Work Journal policy was
created. CA-P-073 owns mechanism-neutral Evaluation coverage for this final
Method set; CA-P-074 and CA-P-075 remain its later completion and closure
steps.
