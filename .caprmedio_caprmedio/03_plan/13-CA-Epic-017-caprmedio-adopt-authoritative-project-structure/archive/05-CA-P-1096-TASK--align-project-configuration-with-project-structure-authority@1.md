---
atom_id: CA-P-1096
cce_version: cce_1
cce_form: obligation
author: Operator
assignee: AI Agent
autonomous_confidence_threshold: 99
subjects:
  governs: "Scope Unit"
  depends_on:
    - "Artifact"
    - "Atom"
    - "Operator"
    - "AI Agent"
    - "Autonomous Confidence Threshold"
version: 1
updated_at: "2026-09-14 23:56:42 +0000"
relations:
  depends_on:
    - CA-P-1095
---
# Align Project Configuration with Project Structure authority

the Assignee **must** align Project Configuration authority with the Core Project Structure model **without** redefining its invariant rules.

## Scope

PROJECT_CONFIGURATION at `.caprmedio_caprmedio/000_CAPRMEDIO_framework/00_APPLICABLE_METHODOLOGY/000_APPLICABLE_MTHD_sources/003_PROJECT_CONFIGURATION`; active RMEDO Atoms governing structure, Settings, default/override selection, concrete bindings, **and** structural Projection consumption.

## Definition of Done

the Task is **not** Done **if** (Project Configuration still turns a structural Projection into authority **or** duplicates concrete manifest values **or** overrides Core invariants **or** defines a second per-unit Authority Mode source **or** loses a valid project-specific expansion during reconciliation).

## Details

review the affected active sources individually. retain genuine project-specific expansions **and** general rules; revise mixed Atoms while preserving distinct Claims **and** exact prior revisions. identify obsolete concrete binding Atoms **and** their references for the later cutover disposition map; do **not** retire the only live binding source before the candidate is validated **and** activation is ready. Framework Instance defaults remain Settings, while per-unit explicit overrides migrate **to** Project Structure. no direct Settings-value edit, Tool change, projection regeneration, **or** unrelated configuration cleanup occurs here.

execute **only** after the direct prerequisite is Done **when** one is declared. **if** confidence is below the effective Autonomous Confidence Threshold, check Project Principles first **and** ask the Operator **if** uncertainty remains. preserve unrelated working-tree changes **and** historical records. creating this Task authorizes no execution by itself.
