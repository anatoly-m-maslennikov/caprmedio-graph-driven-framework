# CA-P-979 Process flow-graph definition

Execution evidence, not source authority. CA-P-978 is Done. All 13 live Project Principles were read and remain unchanged. Confidence is 99%. The Operator resolved the earlier compatibility question with: “not just ordered, it's flow diagram, each node is some action - doer, finder, checker/test, quality gate eval, ect”. This explicit correction supersedes P979's former linear-sequence constraint. P979 Version 2 records that correction, preserves its identity and dependency on P978, and moves to the matching Action-flow-graph filename under `done/`; its exact Version 1 bytes remain in a new archive.

CA-R-1453 Version 1 is the single canonical Process definition in Active CORE_META_MODEL. A Process is a reusable flow graph whose nodes reference Actions; its own explicit control flow governs order and conditional selection or revisiting of nodes. Action definitions are reused without copying or redefining them per Process. The reusable Process definition remains distinct from a particular execution and its Journal Records. Doer, finder, checker/test and quality-gate evaluation illustrate responsibilities of Actions; they do not establish a closed Action or Actor taxonomy.

CA-R-1340 Version 5 makes the adjacent consistency correction from “Process sequence” to “Process definition”. Its Method role continues to own Implementation choices and conventions, excluding authoritative operational Action or Process definitions. No other source boundary changes. The exact Version 4 bytes are preserved in a new archive.

Admission follows CA-R-732 and CA-D-378: the current Project, source, archive and Journal identity scan found R1452 as the maximum Requirement number; no R1453 assignment or reference existed, and the Git-history filename scan found no prior R1453. The admitted current CORE_META_MODEL had no canonical Process definition, so CA-R-126 requires one new owner rather than a synonym or duplicate. R1453 is a Requirement meta-model definition under the P977 primary-contribution boundary in R1282/R1288/R1344; it is not itself an operational Process instance. Local Tier Core follows R659 because it establishes Process's semantic boundary; CORE_META_MODEL's current ancestry gives Global Tier 7. D285 supplies the filename tier token. R1279 supplies the single governed Subject requirement.

The existing `subjects.governs.continuant: Process` representation is temporary carrier compatibility under D269 and M125. It neither assigns Process to the Entity taxonomy nor retains the temporal axis as future authority. No Subject hierarchy or graph relation was invented. Exact relation names, relation domains, edge cardinalities and node-reference schema remain with P980; procedure ownership remains with P982, temporal authority with P984, and carrier migration with P992.

The definition preserves accepted control conditions. Live CA-M-267, CA-M-269 and CA-E-441 require stop on success, up to three additional fix-and-evaluate retries after an initial failure when no Operator override applies, escalation after the final failed retry, earlier confidence/approval escalation where required, and no silent retry-budget reset. Those carriers remain byte-identical. Conditional selection and revisiting are inside Process flow; this is not a restriction to a linear sequence with all flow outside it. No automatic permission for concurrency, arbitrary recursion or unlimited retries follows.

These fixtures are semantic acceptance checks against the complete definition and existing authority; they do not add reusable Operations, Action Types, criteria or implementation behavior.

| Fixture | Result and reason |
|---|---|
| Finder supplies inputs, doer changes a candidate, checker tests it, and quality-gate evaluation chooses the accepted next Action. | Admitted as Action responsibilities connected by explicit Process flow, with conditional selection represented in that flow. |
| A checking Action uses an E Atom's conformance or acceptance criterion. | The checking Action is the node; the E Atom retains its Spec contribution and is not itself an execution node. No criterion is copied. |
| Two Processes use the same sufficient checksum-check Action. | Both reference its one reusable definition; neither copies nor redefines it. |
| One Process is performed twice and produces distinct execution records. | One reusable definition remains distinct from both particular executions and their Journal Records. |
| Nodes are presented only as an unordered collection. | Insufficient: Process requires explicit control flow governing order and applicable conditions. |
| A definition excludes conditional selection or revisiting by forcing all behavior into a linear sequence. | Rejected against the accepted correction: control flow belongs to the Process. |
| Initial evaluation succeeds. | Stop under M267; no automatic repair retry follows. |
| Initial evaluation fails and the next three fix-and-evaluate retries also fail under the default policy. | The Process revisits the repair/check nodes under M269's explicit budget, then escalates; the initial failure does not consume one of the three retries. |
| A permitted retry succeeds before the budget is exhausted. | Stop on success; remaining budget does not authorize extra repair retries. |
| A confidence or approval gate requires escalation, or the failure set changes between retries. | Escalate when required; a changed failure set never resets the accepted retry budget. |
| Node labels doer/finder/checker/quality-gate are treated as exhaustive Action or Actor Types. | Rejected: these are illustrative responsibilities, not a closed taxonomy. |
| A flow graph is used as blanket permission for concurrent execution, arbitrary recursion or unlimited retry. | Rejected: the definition supplies none of those permissions. |

The revised Task Definition of Done falsifying condition is false. Deterministic validation confirms one modified admitted source (R1340), one new canonical source (R1453), 1,557 unselected admitted sources unchanged, all 53 excluded Drafts unchanged, all 8,322 pre-existing archive carriers unchanged, and the immutable P976 evidence plus P977/P978 reports/maps unchanged. The resulting active frontier is 1,559; Active CORE_META_MODEL contains 677 sources. YAML, one definition Claim and one governed Subject, the exact Method wording correction, Task metadata preservation, prior-byte archives and scoped whitespace pass. P980 and later Tasks are not executed.

The changed-source map records exact paths, versions, before/successor/archive hashes, source authority and validation. Its own digest is bound by its canonical Journal event, avoiding self-digest recursion. Seven completed schema-3 Journal events cover R1453, the R1340 archive/update, the P979 archive/move-update, this report and the map. The R1340 and P979 changes reference their existing exact prior-result events; no recovered or fabricated historical event is required. Pre-existing Journal bytes are preserved. The Git index and staged-name digests remain unchanged; D335 Git materialization is pending, and no staging, unstaging, commit, push, runtime, code, settings or generated methodology projection rebuild is claimed.
