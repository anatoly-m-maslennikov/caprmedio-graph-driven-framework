---
cce_version: cce_1
cce_form: concern_question
subjects:
  governs: "Process"
  depends_on:
    - "Action"
    - "Atom"
    - "Relation"
    - "Scope Unit"
    - "Projection"
    - "Operator"
priority: medium
version: 1
updated_at: "2026-09-17 13:20:38 +0000"
llm_session_ids:
  - codex:01a02650-eff7-7453-8c37-0699b36773c6
relations: {}
---
# Which Atom lineage flow replaces ordered Layer propagation?

which admitted Atom-level Relation paths **and** reusable Action bindings preserve CAPRMEDIO-META-REQU-096's required change-propagation outcomes **without** retaining its obsolete ordered-Layer dependency graph?

## Evidence

the source requires finding affected consumers, marking affected current artifacts potentially stale, routing reconciliation **to** the owner, **and** closing affected currentness **or** Evaluation gates. it also protects historical Atoms, rejects Projection-only completion, **and** permits later feedback as a Concern. its initiating rule instead says change propagates **only** forward through an ordered Layer graph. the Operator rejects dependency edges between Scope Units; CA-R-915 also forbids inferring a dependency from Local Order alone. CA-O-058 supplies recursive Atom-lineage assessment, but does **not** by itself define the remaining marking, routing, **and** closure Action bindings.

## Principle check

CA-M-002 requires reuse of existing Actions **and** Relations; CA-M-006 rejects competing Scope-tree **and** dependency semantics; CA-R-1490 protects the still-required consumer dispositions **and** history. CA-R-1453 requires a Process flow whose nodes reference Actions, **not** a renamed procedural paragraph **or** guessed graph edges.

## Disposition

preserve the source **and** defer its unsupported graph-to-flow mapping. retain the specified outcomes during replacement, identify the actual admitted Relation directions, **and** reuse existing Actions **before** adding a justified missing one. do **not** create a new Scope Unit dependency graph **or** claim that a Role rename alone completes this migration.
