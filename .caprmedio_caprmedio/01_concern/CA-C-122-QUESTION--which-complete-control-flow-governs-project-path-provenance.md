---
cce_version: cce_1
cce_form: concern_question
subjects:
  governs: "Process"
  depends_on:
    - "Action"
    - "Tool"
    - "Journal/Record"
    - "Project"
priority: medium
version: 1
updated_at: "2026-09-17 04:01:41 +0000"
llm_session_ids:
  - codex:01a02650-eff7-7453-8c37-0699b36773c6
relations: {}
---
# Which complete control flow governs project-path provenance?

which complete manager-declared control flow **and** Action boundaries replace CA-M-087 **without** inventing aggregate completion, recovery, **or** branch-join semantics?

## Evidence

CA-M-087 defines intake, reconciliation, provisional context, independent Journal **and** real-change branches, later Journal-only commits, persisted states, **and** recovery. CA-R-802 assigns the complete admissible execution graph **to** the pure manager **and** permits independent non-Git work; CA-R-805 owns the shared Git gate, CA-R-812 owns append behavior, **and** CA-R-1385 owns the autonomy boundary. CA-O-037 already defines the reusable envelope check.

the source establishes worker responsibilities but does **not** enumerate the full manager-selected branch-join **and** terminal-state conditions: for example, when a path action becomes complete relative **to** its later Journal-only batch, which recovery transition follows **every** interrupted branch, **and** how those results become reusable Action outputs rather than Tool-owned decisions. CA-R-1453 requires explicit Action references **and** control flow; merely relabeling the existing ordered paragraph as a Process would **not** satisfy that model.

## Principle check

CA-M-002 requires reuse of the accepted envelope **and** worker authority. CA-M-006 requires coherent transitions, CA-M-005 rejects invented state machinery **and** unnecessary new Entities, **and** CA-R-1490 requires preservation of pending work **and** historical evidence. they forbid making one branch wait on another merely **to** simplify a diagram, but do **not** supply the missing aggregate completion **and** recovery conditions.

## Disposition

deferred under the Operator's uncertainty policy. preserve CA-M-087's exact current behavior **and** all existing Evaluation targets. resolve the complete admitted flow first, **then** extract O Action definitions **and** a Process that references them, keeping independent construction conventions **in** M **and** concrete state encoding **in** D. do **not** count this Concern as a completed M-to-O migration, implement a new scheduler, **or** infer that existing Tool code supplies missing governing authority.
