# CA-P-957 batch 01 — concrete Carrier and procedure classifications

Non-authoritative execution evidence. Batch 01 was applied only after root GO for the immutable plan SHA-256 `e8eed3bbf7b019b1ce8d3a333d9f64dcc9b4326d977fe4c05a60002df8cf6b64`. This is a partial checkpoint, not completion of CA-P-957.

## Result

The batch records 77 reasoned Standard dispositions: 68 Core-to-Standard revisions and 9 unchanged Standard Atoms. Each revised Carrier has its exact complete prior bytes in a new `@<version>` Archive; no existing Archive was overwritten. Every complete body, Subject, identity, owner, Scope binding and direct relation is unchanged. The only current-byte changes are incremented `version` and `updated_at`; the current filename omits the old CORE tier token.

The governing distinction is CA-R-660@10 and the concrete-choice witness procedure in CA-M-272@1. The records identify each exact inspected clause and a replacement of its selected representation or procedure that retains the represented semantics. Prior Core labels and CORE_META_MODEL ownership supplied no classification evidence. Definitions such as D357 are classified from their own concrete representation boundary, not from the name of the defined value.

The complete 620-origin review ledger contains no duplicate identity: 77 are recorded in this checkpoint and 543 still require final dispositions. The source frontier remains 707 current source/adjacent Atoms, of which 652 are CORE_META_MODEL. The 32 CA-P-956 active results remain excluded from CA-P-957 ownership. All 639 current records outside this batch's 68 revisions still match their preceding authorized bytes.

## Frozen and applied evidence

- `CA-P-957-batch-01-dispositions.json`: complete body, clause, reason, replacement witness, identity/revision/hash, owner/Scope/binding, tier/rank and relation impact for every reviewed Atom.
- `CA-P-957-batch-01-planned-source-delta.json`: immutable exact before/proposed bytes and path/version/Archive checkpoint.
- `CA-P-957-batch-01-applied-source-delta.json`: actual current and Archive path/version/hash mapping.
- `CA-P-957-batch-01-full-basename-references.json`: literal basename audit over 10,071 repository Markdown files.
- `CA-P-957-master-review-ledger.json`: every one of the 620 original remaining identities, without batch overlap.

For D267, D278 and D299, the hypothetical extra representation in the frozen witness means a derived, non-authoritative copy with maintained equality, refresh and inheritance behavior. It never means a second independently editable authority source. These counterfactuals demonstrate separation of encoding from semantic ownership; they do not approve a live alternative Carrier contract. The frozen witness records are preserved unchanged.

Requirement tier-parent constraints R796/R829 apply to Requirement Atoms. The batch contains Delivery Atoms and M235; its legitimate existing content-boundary relations are preserved. No relation was removed merely because its endpoints now have the same tier. Stable-ID references remain valid. The 58 literal old-basename references found in Markdown are all generated Applicable Methodology source-path metadata; their later governed regeneration is outside this source-only Task. There are no current-source or other non-generated Markdown full-basename references requiring a rewrite for this batch. This audit does not claim to cover arbitrary non-Markdown runtime consumers or external systems.

## Verification and provenance

Run from the repository root:

```sh
python3 -B .caprmedio_caprmedio/101_LAYER_1_FRAMEWORK_METHODOLOGY/03_plan/01-CA-Epic-005-FRAMEWORK_METHODOLOGY-adopt-core-standard-detail-methodology-tiers/execution_evidence/CA-P-957-evidence.py --check-applied-batch 01
```

PASS: 68 exact current candidates, 68 byte-exact prior Archives, 9 unchanged dispositions, exact mapped frontier membership, preserved bodies and relations, version increments and absent old current paths. The checker consumes explicit applied-batch mappings. Its former pre-mutation mode now rejects an already applied batch rather than treating historical hashes as current closure.

Root established 57 truthful observed-state baselines and reused 11 matching prior results before GO. Root then sealed 136 source result events: 68 current revisions and 68 prior Archive additions. This worker independently verified every event's result path and SHA-256 against the exact current bytes and applied mapping. Receipts are bound in the applied delta: `anatoly-m-maslennikov-2026-09-10-part-2.ndjson` rows 83–100, `part-3.ndjson` rows 1–100 and `part-4.ndjson` rows 1–18 under the Project Work Journal. No claim is made that the historical actor/time of preexisting dirty bytes was reconstructed. No Git action, Tool/runtime edit, generated Projection edit, Scope Unit folder migration, Task/lifecycle change or installed runtime/E2E execution occurred in this batch. Root also reported zero active canonical relation diagnostics for the batch results.

The still-unclassified corpus, R1053 boundary allocation, and any further direct reclassification consequences remain open. Confidence is at least 99% for this reviewed batch and its stated preservation boundary.
